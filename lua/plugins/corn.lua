local M = {}

---NOTE: This is nice, but it always gives me an error that it cannot close an unnamed file

---@type LazySpec
M.spec = {
  "RaafatTurki/corn.nvim",
  enabled = false,
  config = true,
}

return M.spec
