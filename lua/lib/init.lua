local M = {}

M = {
  copy = require("lib.copy"),
  log = require("lib.log"),
  utils = require("lib.utils"),
  tmux = require("lib.tmux"),
  component = require("lib.component"),
  git = require("lib.git"),
}

return M
